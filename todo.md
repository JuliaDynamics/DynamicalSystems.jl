* Add much more systems in the `famous_systems.jl`
* Ensure type stabilty across all systems
  * Ensure that `u` and `J` has same `eltype`
  * Add tests that create stuff with `BigFloat` and other types
  
